using Schichtplaner.Models;

namespace Schichtplaner.Services;

public interface ISchichtService
{
    Task<(bool Success, string Message)> ValidateSchichtAsync(Schicht schicht, int? excludeSchichtId = null);
    Task<decimal> GetMonatsstundenAsync(int mitarbeiterId, int jahr, int monat, int? excludeSchichtId = null);
}
